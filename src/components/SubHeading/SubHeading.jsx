import React from 'react';
import { images } from '../../constants'

const SubHeading = ( {title}) => (
  <div className='goldBar' style={{ marginBottom: '1rem' }}>
    <p className='p__cormorant'>{title}</p>
    <div />
  </div>
);

export default SubHeading;
